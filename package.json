{
  "name": "fe-proxy",
  "version": "0.0.1",
  "description": "for local request mock",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "es6-promisify": "^6.0.0",
    "yargs": "^11.0.0"
  }
}
